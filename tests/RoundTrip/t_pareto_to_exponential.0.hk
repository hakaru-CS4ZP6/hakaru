#If X ~ Pareto(1, λ) then log(X) ~ Exp(λ).

def pareto(lambda prob, kappa prob):
	X <~ uniform(0,1)
	return lambda / (real2prob(X) ** (1/kappa))

def paretoToExponential(alpha prob):
	X <~ pareto(1/1,1/alpha)
	return real2prob(log(X))

paretoToExponential(1/1)