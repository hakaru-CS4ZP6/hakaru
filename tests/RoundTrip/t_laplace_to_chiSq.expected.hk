def exponential(alpha prob):
	X <~ uniform(0,1)
	return -1 * alpha * log(real2prob(X))

def Laplace(alpha prob, beta prob):
	X <~ exponential(alpha)
	Y <~ exponential(beta)
	return X - Y

X <~ Laplace (0,1)
return (2 * abs(X))/ 1 